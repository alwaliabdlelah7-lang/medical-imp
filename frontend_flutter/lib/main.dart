import 'package:flutter/material.dart';
void main() => runApp(MyApp());
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) => MaterialApp(title: 'IMP Medical', home: Scaffold(body: Center(child: Text('النظام الطبي المتكامل'))));
}
